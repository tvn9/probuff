syntax = "proto3";

// Money holds currency code, whold number and practional part 
message Money {
   string USD = 1; // 3 letter currency code in CAPITAL 
   int64 integral = 2; // integral value exmaple: 1-999
   int32 decimal = 3; // decimal value example: .22
}
